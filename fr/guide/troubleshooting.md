# Dépannage

```bash
npx tsx packages/core/src/cliEntry.ts doctor
npx tsx packages/core/src/cliEntry.ts status
export COMMANDER_DEBUG=true
```

Provider manquant → exporter la clé. Rate limit → attendre / 2ᵉ clé. Circuit breaker → `doctor --reset`.

[FAQ](/fr/guide/faq)
