# 프로덕션 준비

서킷 브레이커 · DLQ · Saga · WAL 체크포인트 · 시맨틱 캐시 · 멀티 테넌트 · Prometheus.

```bash
curl http://localhost:4000/health/detailed
curl http://localhost:4000/metrics
```

6700+ tests. [배포](/ko/deployment)
